/**
 * Entry resolved as `render-pdf.ts` (webpack tries `.ts` before `.tsx` for extensionless imports).
 * Implementation lives in `render-owner-statement-pdf.tsx` (JSX).
 */
export { renderOwnerStatementPdf } from "./render-owner-statement-pdf"
